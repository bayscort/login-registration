spring:
  profiles:
    active: dev
  cloud:
    config:
      enabled: false
      fail-fast: true
      retry:
        multiplier: 1
        initial-interval: 1000
        max-attempts: 100
        max-interval: 2000
      uri: ${ENV_CONFIG_SERVER_URI:http://172.18.185.110:32080/general/config}
      username: ${ENV_SPRING_USER:root}
      password: ${ENV_SPRING_PWD:s3cr3t}
      name: ${spring.application.name}
      profile: ${spring.profiles.active}